import styled from 'styled-components';

export const ViewFormWrapper = styled.div`
  margin: auto;
  background-color: #FFFFFF;
  width: auto;
  margin: auto;
  margin-top: 30px;

  max-width: 900px;
  min-width: 720px;
  padding: auto;
  border-radius: 25px;
  box-shadow: 0 5px 15px -10px rgba(0, 0, 0, 0.3);

`;